{
  "name": "package-tracking-template",
  "description": "Use the package tracking template to provide the package status within a chat window.",
  "version": "0.0.1",
  "author": "ChatBot.com",
  "scripts": {
    "start": "firebase serve --only functions:packageTrackingBot",
    "deploy": "firebase deploy --only functions:packageTrackingBot"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.16.4",
    "firebase-admin": "^7.3.0",
    "firebase-functions": "^0.5.7"
  },
  "engines": {
    "node": "8"
  }
}
